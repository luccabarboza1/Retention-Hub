services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ombudsman
      POSTGRES_PASSWORD: ombudsman
      POSTGRES_DB: ombudsman
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
